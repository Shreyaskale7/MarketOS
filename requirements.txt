# MarketOS - Python 3.10+
# Tested and pinned versions - do not upgrade without testing

# Web framework
Flask==3.1.3
flask-cors==6.0.2
gunicorn==22.0.0

# Authentication
PyJWT==2.8.0
bcrypt==4.1.3

# Data & ML
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.4.2
scipy==1.13.1

# Gradient boosters — part of the 4-way model tournament in
# ml_forecast_engine.train_horizon_models() (RF, Ridge, XGBoost, LightGBM;
# top-3 by walk-forward IC ensembled). The import is wrapped in try/except
# so training silently degrades to a 2-way RF/Ridge tournament if these are
# missing — which is exactly what would have happened on every fresh
# install (including Render) since they were previously used in code but
# never declared here. Versions pinned to what is confirmed working locally.
xgboost==3.2.0
lightgbm==4.6.0

# Market data
yfinance==0.2.54
requests==2.32.3
# Browser-TLS-impersonating HTTP client, used to fetch through yfinance's
# `session=` param. Yahoo Finance's bot detection now fingerprints the raw
# `requests` library itself, not just request volume — shared CI/PaaS IP
# ranges (GitHub Actions, Render free tier) get blocked hardest since many
# unrelated yfinance jobs share the same pool and keep it permanently
# flagged. curl_cffi impersonates a real Chrome TLS handshake to get past it.
curl_cffi==0.15.0

# Database
SQLAlchemy==2.0.30
psycopg2-binary==2.9.9

# Utilities
python-dotenv==1.0.1
apscheduler==3.10.4
python-dateutil==2.9.0.post0
pytz==2025.1

Flask-Limiter==3.8.0
